<header class='navbar-default navbar-fixed-top '>
	<div class='container'>
		<div class="navbar-header">
		
			<% if user_signed_in? %>

			<button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
				<span class="sr-only">Toggle navigation</span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
			</button>
			<% end %>

			<%= link_to 'Sistema microblogging', root_path, class: 'navbar-brand' %>			
		</div>
		
		<% if user_signed_in? %>
		<nav id="navbar" class="navbar-collapse collapse">
			<ul class="nav navbar-nav navbar-right">
				<li>
					<%= link_to 'Nuevo Mensaje', new_post_path, class: 'icon-new-message' %>	
				</li>

				<li class="dropdown">
					<a href="#" class="dropdown-toggle icon-user" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
						<%= current_user.username %> 
						<span class="caret"></span>
					</a>

					<ul class="dropdown-menu">

						<li>
							<%= link_to 'Ver perfil', "/users/#{current_user.id}" %>
						</li>
						<li><%= link_to 'Mis grupos', root_path  %></li>

						<li role="separator" class="divider"></li>
						<li><%= link_to 'Configuracion', edit_user_registration_path %></li>

						<li role="separator" class="divider"></li>
						<li><%= link_to 'Cerrar sesión', destroy_user_session_path, method: :delete %></li>
					</ul>
				</li>
				
			</ul>
		</nav>
		<%end%>
	</div>	
</header>