<div class='container-fluid'>
	
	<%= render 'aside'%>

	<section class='col-md-8 col-lg-9'>
		<% if @posts.any? %>
		<!-- Listamos los posts -->
		<%= render partial: 'posts/post', collection: @posts %>
		<%else%>
		<%= render 'posts/empty'%>
		<%end%>
	</section>

</div>

